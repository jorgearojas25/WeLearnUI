const config = {
  baseURL: "http://localhost:8080/",
  services: {
    courses: "courses/",
    resources: "resources/",
    resourcesPost: "resources/video/",
    users: "users/",
  },
};

export default config;
