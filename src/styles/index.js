import colors from "./colors";
import fonts from "./fonts";
import spacing from "./spacing";

const theme = {
  colors: { ...colors },
  fonts: { ...fonts },
  spacing: { ...spacing },
};

export default theme;
